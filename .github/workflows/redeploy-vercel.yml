name: Redeploy Vercel

# Manually trigger a production redeploy of the frontend on Vercel.
# Requires the repository secret VERCEL_DEPLOY_HOOK (a Vercel Deploy Hook URL
# for the main branch: Vercel -> Project Settings -> Git -> Deploy Hooks).
on:
  workflow_dispatch:

# The job only calls the deploy hook: it needs no GitHub token permissions.
permissions: {}

# Pressing "Run" twice queues one redeploy instead of starting two.
concurrency:
  group: redeploy-vercel
  cancel-in-progress: false

jobs:
  redeploy:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Trigger Vercel deploy hook
        env:
          VERCEL_DEPLOY_HOOK: ${{ secrets.VERCEL_DEPLOY_HOOK }}
        run: |
          if [ -z "$VERCEL_DEPLOY_HOOK" ]; then
            echo "::error::Repository secret VERCEL_DEPLOY_HOOK is not set"
            exit 1
          fi
          curl --fail --silent --show-error --max-time 30 --retry 2 -X POST "$VERCEL_DEPLOY_HOOK"
          echo "Vercel redeploy triggered"
